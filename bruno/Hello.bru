meta {
  name: Hello
  type: http
  seq: 6
}

get {
  url: http://localhost:8080/hello?name=World
  body: none
  auth: none
}

tests {
  test("should return status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("should return greeting message", function() {
    expect(res.getBody()).to.equal("Hello World");
  });
}
